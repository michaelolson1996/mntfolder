
import styles from './ImageUploader.module.css';

export default function ImageUploader() {
    return (
        <div>
            {/* <div name={'imageupload-section__close'}>
                <div id={styles.newpost_imageuploader__main} onClick={(e) => {
                    if (!e.target.attributes["name"] || e.target.attributes["name"].value !== 'imageupload-section__close') {
                        e.stopPropagation();
                    };
                }}>
                    
                    
                </div>
            </div> */}
        </div>
    );
};